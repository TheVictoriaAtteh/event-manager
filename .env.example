# Frontend API configuration
# Keep this relative when Vite/the deployed web host proxies /api to the API.
VITE_API_URL=/api

# Used only by the local Vite development proxy; never exposed to the browser.
VITE_BACKEND_PROXY_TARGET=http://localhost:4000
